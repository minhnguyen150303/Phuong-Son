<!DOCTYPE html>
<html>

<head>
    <title>Danh Sách Users</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

    <style>
        /* Cấu hình chung cho các nút */
        .btn-sua,
        .btn-xoa {
            padding: 8px 16px;
            font-size: 14px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s;
            text-decoration: none;
            /* Xóa gạch dưới của liên kết */
            color: white;
            /* Màu chữ trắng */
            display: inline-block;
            text-align: center;
        }

        /* Nút "Sửa" */
        .btn-sua {
            background-color: #4CAF50;
            /* Màu xanh lá */
        }

        .btn-sua:hover {
            background-color: #45a049;
        }

        /* Nút "Xóa" */
        .btn-xoa {
            background-color: #f44336;
            /* Màu đỏ */
        }

        .btn-xoa:hover {
            background-color: #e53935;
        }

        /* Hiệu ứng khi nhấn nút */
        .btn-sua:active,
        .btn-xoa:active {
            transform: scale(0.98);
            /* Thu nhỏ khi nhấn */
        }

        /* Nút "Thêm" */
        .btn-them {
            background-color: #2196F3;
            /* Màu xanh dương */
            color: white;
            /* Màu chữ trắng */
            padding: 10px 20px;
            font-size: 14px;
            font-weight: bold;
            text-decoration: none;
            /* Xóa gạch chân */
            border-radius: 5px;
            cursor: pointer;
            display: inline-block;
            margin-top: 20px;
            /* Tạo khoảng cách phía trên */
            transition: background-color 0.3s ease, transform 0.2s;
            position: relative;
            /* Giúp căn chỉnh chính xác */
            left: 0;
            /* Đặt về bên trái */
        }

        /* Hiệu ứng hover nút "Thêm" */
        .btn-them:hover {
            background-color: #1976D2;
            /* Màu xanh dương đậm hơn */
        }

        /* Hiệu ứng khi nhấn nút */
        .btn-them:active {
            transform: scale(0.98);
            /* Hiệu ứng thu nhỏ khi nhấn */
        }
    </style>
</head>


<body>
    <div class="w3-container">
        <h2>Danh sách người dùng</h2>

        <table class="w3-table-all w3-hoverable">
            <thead>
                <tr class="w3-light-grey">
                    <th>Email</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Password</th>
                    <th>Action</th>
                </tr>
            </thead>

            <% for(var i=0; i < dataTable.length; i++) { %>
                <tr>
                    <td>
                        <%= dataTable[i].email %>
                    </td>
                    <td>
                        <%= dataTable[i].firstName %>
                    </td>
                    <td>
                        <%= dataTable[i].lastName %>
                    </td>
                    <td>
                        <%= dataTable[i].password %>
                    </td>
                    <td>
                        <a class="btn-sua" href="/edit-crud?id=<%= dataTable[i].id %>">Sửa</a>
                        <a class="btn-xoa" href="/delete-crud?id=<%= dataTable[i].id %>">Xóa</a>
                    </td>
                </tr>
                <% } %>
        </table>
        <a class="btn-them" href="crud">Thêm</a>
    </div>

</body>

</html>