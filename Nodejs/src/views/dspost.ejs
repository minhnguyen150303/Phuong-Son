<!DOCTYPE html>
<html>

<head>
    <title>Danh Sách Posts</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

    <style>
        /* Cấu hình chung cho các nút */
        .btn-sua,
        .btn-xoa,
        .btn-chitiet {
            padding: 8px 16px;
            font-size: 14px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s;
            text-decoration: none;
            color: white;
            display: inline-block;
            text-align: center;
        }

        /* Nút "Chi tiết" */
        .btn-chitiet {
            background-color: #FFC107;
            /* Màu vàng */
        }

        .btn-chitiet:hover {
            background-color: #ffca28;
            /* Màu vàng đậm */
            transform: scale(1.05);
            /* Phóng to nhẹ khi hover */
        }

        /* Nút "Sửa" */
        .btn-sua {
            background-color: #4CAF50;
            /* Màu xanh lá */
        }

        .btn-sua:hover {
            background-color: #45a049;
        }

        /* Nút "Xóa" */
        .btn-xoa {
            background-color: #f44336;
            /* Màu đỏ */
        }

        .btn-xoa:hover {
            background-color: #e53935;
        }

        /* Hiệu ứng khi nhấn nút */
        .btn-sua:active,
        .btn-xoa:active,
        .btn-chitiet:active {
            transform: scale(0.98);
            /* Thu nhỏ khi nhấn */
        }

        /* Nút "Thêm" */
        .btn-them {
            background-color: #2196F3;
            /* Màu xanh dương */
            color: white;
            /* Màu chữ trắng */
            padding: 10px 20px;
            font-size: 14px;
            font-weight: bold;
            text-decoration: none;
            /* Xóa gạch chân */
            border-radius: 5px;
            cursor: pointer;
            display: inline-block;
            margin-top: 20px;
            /* Tạo khoảng cách phía trên */
            transition: background-color 0.3s ease, transform 0.2s;
            position: relative;
            /* Giúp căn chỉnh chính xác */
            left: 0;
            /* Đặt về bên trái */
        }

        /* Hiệu ứng hover nút "Thêm" */
        .btn-them:hover {
            background-color: #1976D2;
            /* Màu xanh dương đậm hơn */
        }

        /* Hiệu ứng khi nhấn nút */
        .btn-them:active {
            transform: scale(0.98);
            /* Hiệu ứng thu nhỏ khi nhấn */
        }
    </style>


</head>


<body>
    <div class id="dieuhuong">
        <%- include('./dieuhuong') %>
    </div>

    <div class="w3-container">
        <h2>Danh sách bài viết</h2>

        <table class="w3-table-all w3-hoverable">
            <thead>
                <tr class="w3-light-grey">
                    <th>ID</th>
                    <th>Tittle</th>
                    <th>Content</th>
                    <th>AdminID</th>
                    <th>Type</th>
                    <th>Action</th>
                </tr>
            </thead>

            <% for(let i=0; i < dataTable.length; i++) { %>
                <tr>
                    <td>
                        <%= dataTable[i].id %>
                    </td>
                    <td>
                        <%= dataTable[i].title %>
                    </td>
                    <td>
                        etc..
                    </td>
                    <td>
                        <%= dataTable[i].adminId %>
                    </td>
                    <td>
                        <%= dataTable[i].type %>
                    </td>
                    <td>
                        <a class="btn-chitiet" href="/get-chitiet?id=<%= dataTable[i].id %>">CHi tiết</a>
                        <a class="btn-sua" href="/edit-post?id=<%= dataTable[i].id %>">Sửa</a>
                        <a class="btn-xoa" href="/delete-post?id=<%= dataTable[i].id %>">Xóa</a>
                    </td>
                </tr>
                <% } %>
        </table>
        <a class="btn-them" href="post">Thêm</a>
    </div>

</body>

</html>